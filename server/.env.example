# Copy to .env and adjust. The defaults match a local Homebrew PostgreSQL
# install, where the superuser role is your own macOS username.
DATABASE_URL=postgresql://localhost:5432/pawsome
PORT=4000

# Origin allowed to call the API. Vite dev server by default.
CORS_ORIGIN=http://localhost:5173,http://localhost:4173
